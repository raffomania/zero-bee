image: node:latest
stages:
  - pages
pages:
  stage: pages
  script:
    - npm install
    - npx parcel build -d public --public-url ./ index.html
  artifacts:
    paths:
      - public
  only:
    - master
    
